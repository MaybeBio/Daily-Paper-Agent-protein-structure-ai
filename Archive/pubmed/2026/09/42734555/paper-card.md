## 01 基本信息

- **标题**：SA-MPNN: A Sequence-Aware ThermoMPNN for Accurate Prediction of Mutational Effects on Protein Thermodynamic Stability
- **作者**：Zhang, Xin Yue; Zheng, Xiang; Dong, Ze Yuan; Xiao, Jing; Zhang, Hao; Wu, Min Cong; Liu, Zhao Ming; Li, Guo Hui; Li, Jiao; Wang, Wei Bu; Liang, Yu; Li, Qi Ming; Su, Ji Guo
- **单位**：未提供（PubMed 记录未列出单位信息）
- **期刊/平台**：Journal of Chemical Information and Modeling (JCIM)
- **年份**：2026（在线日期 2026-09-14）
- **论文类型**：方法学 + 湿实验验证（端到端混合框架 + 案例验证）
- **领域**：蛋白质热稳定性预测 × 深度学习（逆折叠模型 + 蛋白质语言模型）
- **关键词**：protein thermodynamic stability, mutation effect prediction, inverse folding models, protein language models, ThermoMPNN, ESM2, self-attention fusion
- **DOI/arXiv**：10.1021/acs.jcim.6c01644
- **代码**：未提供
- **数据**：Megascale 训练集；多个 benchmark 数据集（具体名称未在摘要中列出）；湿实验数据（AtBgl1A 突变体 Tm 值）
- **阅读日期**：2026-06-02（按当前日期推算）
- **在该方向中的位置**：本文位于「蛋白质结构相关计算研究 × AI 方法」交叉点，具体属于「突变效应/热稳定性预测」子方向。它直接扩展了 ThermoMPNN（逆折叠模型微调用于稳定性预测）的架构，通过引入 ESM2 序列表示和自注意力融合机制，将序列进化信息与结构几何信息结合。该工作处于「结构预测/设计 × 序列模型融合」的前沿，与 AlphaFold 系列（结构预测）、ESM 系列（序列建模）、MPNN 系列（逆折叠）均有方法学关联。

---

## 02 一句话总结

本文提出 SA-MPNN，一个将 ESM2 序列表示通过自注意力机制动态融合进 ThermoMPNN 架构的轻量级端到端框架，在 Megascale 上训练后，在多个 benchmark 上相对 ThermoMPNN 取得 modest but consistent 的稳定性预测提升，并通过 AtBgl1A 湿实验验证（最优突变体 GC20 的 Tm 提升 5.97°C）支持其实际应用价值。

---

## 03 研究问题

- **具体问题**：如何更准确地预测单点突变对蛋白质热力学稳定性（ΔΔG 或 Tm 变化）的影响？
- **为什么重要**：热稳定性预测是蛋白质工程（治疗性抗体、工业酶设计）的核心环节，准确预测可大幅减少湿实验筛选成本。
- **现有方法为何不足**：
  - 基于结构的物理模拟方法（如 MD、FoldX）计算成本高且精度有限。
  - 逆折叠模型（IFMs）如 ThermoMPNN 通过微调可达到 SOTA，但 IFM 本质上是结构→序列的映射模型，其内部表示主要编码几何/化学环境，对蛋白质的深度进化信息（序列同源家族中的保守性、共变异模式）捕获能力有限。
  - 蛋白质语言模型（pLMs）如 ESM2 擅长捕获进化信息，但缺乏显式三维结构感知。
- **精确研究问题**：Can a hybrid framework that dynamically integrates pLM sequence representations into a ThermoMPNN architecture achieve consistent and practically meaningful improvements in mutation thermostability prediction over the structure-only ThermoMPNN baseline?

---

## 04 背景与发展脉络

> 注：以下脉络基于本文摘要及该领域公开知识构建，标注为「经外部核验 + 本文框架」混合。

| 阶段 | 代表性方法 | 优点 | 局限 | 本文位置 |
|------|-----------|------|------|---------|
| 第一阶段：物理/统计势 | FoldX, Rosetta, MD 模拟 | 有物理可解释性 | 计算昂贵、精度受力场限制 | 作为对比基线 |
| 第二阶段：序列进化特征 | EVmutation, DeepSequence | 捕获共进化信号 | 缺乏结构信息、难以泛化到新折叠 | 本文的 pLM 分支继承其进化信息思想 |
| 第三阶段：结构深度学习 | DDG预测网络（如 DDGPred, ThermoNet） | 端到端、速度快 | 依赖结构特征工程、泛化有限 | 作为背景 |
| 第四阶段：逆折叠模型微调 | ThermoMPNN (2023) | 利用结构→序列的丰富表示，SOTA | 缺乏深度进化信息 | 本文的直接基线 |
| 第五阶段：pLM + 结构融合 | ESM-IF, ProstT5, 本文 SA-MPNN | 同时利用序列进化 + 结构几何 | 融合策略需精心设计 | **本文所在位置** |

**本文主张的位置**：在 ThermoMPNN 基础上，通过自注意力机制动态融合 ESM2 序列表示，以最小的架构改动获得一致的精度提升，并首次在该框架下提供湿实验验证。

---

## 05 核心痛点

| 痛点 | 表现 | 成因或作者解释 | 文中证据 |
|------|------|---------------|---------|
| IFM 缺乏深度进化信息 | ThermoMPNN 在捕获序列同源家族保守性、共变异模式方面不足 | 逆折叠模型训练目标是结构→序列重建，其隐空间主要编码局部几何/化学环境，而非跨物种进化约束 | 摘要原文："IFMs are limited in their capacity to capture protein deep evolutionary information" |
| pLM 缺乏结构感知 | ESM2 等序列模型无法直接利用三维结构上下文 | 序列模型仅以氨基酸序列为输入，无坐标/距离信息 | 摘要原文："protein language models (pLMs) excel"（在进化信息方面），暗示其结构盲区 |
| 两种模态简单拼接效果不佳 | 直接 concatenation 或加权求和无法有效融合异构特征 | 序列表示（1D 进化信号）与结构表示（3D 几何信号）分布差异大，需要自适应融合机制 | 摘要原文："By evaluating various feature fusion strategies, we selected a self-attention-based integration mechanism"（暗示其他策略效果较差） |
| 预测精度提升幅度有限 | 相对 ThermoMPNN 仅 modest gains | 热稳定性预测本身信噪比低，且 ThermoMPNN 已接近性能上限 | 摘要原文："achieved modest but consistent gains" |
| 计算资源约束 | 大型 pLM 推理成本高 | ESM2 等模型参数量大 | 摘要原文强调 "lightweight" 设计，暗示对效率的考量 |

---

## 06 核心思想

### 1) 表面方法

SA-MPNN 是一个端到端混合框架：
- 输入：蛋白质三维结构（原子坐标）+ 对应氨基酸序列
- 结构分支：ThermoMPNN 编码器（基于 MPNN 的图神经网络），提取每个残基的几何/化学环境表示
- 序列分支：ESM2 编码器，提取每个残基的进化上下文表示
- 融合模块：自注意力机制动态整合两种表示
- 输出：每个残基位点的突变稳定性效应预测（ΔΔG 或相关打分）
- 训练：在 Megascale 数据集上端到端训练

### 2) 核心洞察

**洞察一**：逆折叠模型（IFM）的隐空间虽然富含结构信息，但其训练目标（序列重建）导致表示偏向"可设计性"而非"稳定性"，因此需要外部序列模型补充进化约束信息。

**洞察二**：序列进化信息和结构几何信息是互补而非冗余的——进化信息编码了自然选择压力下的保守性约束，结构信息编码了物理化学环境，二者的交集（即"进化上保守且结构上关键的残基"）往往对稳定性贡献最大。

**洞察三**：自注意力融合优于简单拼接，因为它允许模型按残基位置动态决定序列/结构信息的权重——某些位点（如活性位点）更依赖结构信息，而某些位点（如表面残基）更依赖进化保守性。

### 3) 可能的普适教训 [Analysis]

- **模态融合的"动态加权"原则**：当融合两种异构信息源时，固定权重（拼接、求和）不如让模型学习按位置/上下文动态分配注意力。这一原则可迁移到其他蛋白质性质预测任务（如溶解度、表达量、结合亲和力）。
- **"轻量级增强"策略**：在已有 SOTA 模型上做最小侵入式修改（加一个融合模块），而非重新设计架构，可降低训练成本并保留原模型已学到的丰富表示。
- **湿实验闭环验证**：计算预测 + 湿实验验证的闭环是说服力最强的证据链，尤其对于工程应用导向的方法。

---

## 07 方法总览

**输入**：
- 蛋白质三维结构（PDB 格式，含原子坐标）
- 对应氨基酸序列（一维）

**输出**：
- 每个残基位点的突变稳定性效应预测（ΔΔG 或热稳定性变化打分）

**模块**：
1. **结构编码器**：ThermoMPNN 的 MPNN 编码器，以残基为节点、以距离/方向为边的图神经网络，输出每个残基的结构上下文嵌入
2. **序列编码器**：ESM2（蛋白质语言模型），以序列为输入，输出每个残基的进化上下文嵌入
3. **自注意力融合模块**：将两种嵌入通过多头自注意力机制动态整合，生成融合表示
4. **预测头**：MLP 将融合表示映射为稳定性变化预测值

**训练**：
- 数据集：Megascale（大规模突变稳定性数据集）
- 损失函数：回归损失（如 MSE）用于 ΔΔG 预测
- 端到端训练：两个编码器 + 融合模块 + 预测头联合优化

**工具**：未提供具体框架信息（PyTorch 等未提及）

**假设**：
- 假设 ThermoMPNN 的结构表示和 ESM2 的序列表示在融合后能互补提升稳定性预测
- 假设自注意力机制能有效学习两种模态的权重分配

**文字流程**：
```
输入结构 → ThermoMPNN 编码器 → 结构嵌入 {h_struct_i}
输入序列 → ESM2 编码器 → 序列嵌入 {h_seq_i}
{h_struct_i} 与 {h_seq_i} → 自注意力融合模块 → 融合嵌入 {h_fused_i}
{h_fused_i} → MLP 预测头 → 每个位点的稳定性变化预测
```

---

## 08 核心模块拆解

| 模块 | 功能 | 为何需要 | 输入输出 | 支撑证据 | 移除后的已知或预期影响 |
|------|------|---------|---------|---------|----------------------|
| ThermoMPNN 编码器 | 提取残基的结构/几何上下文 | 提供三维结构信息，是稳定性预测的基础 | 输入：原子坐标+残基类型；输出：每残基结构嵌入 | 摘要称 ThermoMPNN 为 SOTA 基线 | **预期**：移除后失去结构感知，性能大幅下降（未提供消融数据） |
| ESM2 编码器 | 提取残基的进化上下文 | 补充 IFM 缺乏的深度进化信息 | 输入：氨基酸序列；输出：每残基序列嵌入 | 摘要称 pLM 在进化信息方面 excel | **预期**：移除后回到纯 ThermoMPNN，性能下降至基线水平（未提供消融数据） |
| 自注意力融合模块 | 动态整合结构+序列两种表示 | 简单拼接无法有效融合异构特征，需要自适应权重 | 输入：两种嵌入；输出：融合嵌入 | 摘要称评估多种融合策略后选择自注意力 | **预期**：替换为简单拼接/求和会导致性能下降（作者评估了其他策略但未给出具体数据） |
| MLP 预测头 | 将融合表示映射为稳定性预测 | 将高维表示转换为标量预测 | 输入：融合嵌入；输出：ΔΔG 预测 | 标准回归头设计 | **预期**：替换为其他回归头影响较小（未提供消融数据） |

> 注：所有消融效应均为「预期效应」，摘要中未提供具体消融实验数据。

---

## 09 关键公式符号

不适用。摘要中未提供任何数学公式或符号定义。方法细节（如自注意力机制的具体公式、损失函数形式）在摘要中未披露，需查阅全文。

---

## 10 实验设计与证据链

**数据集/群体**：
- 训练集：Megascale（大规模突变稳定性数据集，具体规模未在摘要中给出）
- 评估集：多个 benchmark 数据集（具体名称未在摘要中列出）
- 湿实验验证：Acetivibrio thermocellus β-glucosidase（AtBgl1A）

**指标**：
- 相关性分析指标（如 Pearson/Spearman 相关系数）
- 筛选导向评估（如 top-k 突变体的命中率）
- 湿实验：Tm 值（熔解温度）变化

**基线**：
- ThermoMPNN（主要基线）

**评测协议**：
- 在多个 benchmark 上对比 SA-MPNN 与 ThermoMPNN
- 湿实验：选择 top-ranking 突变体进行表达和 Tm 测定

**实验列表**：

| 实验 | 检验的 claim | 对比与条件 | 结果 | 支持的结论 | 不支持更强的结论 | 来源 |
|------|-------------|-----------|------|-----------|----------------|------|
| Benchmark 相关性评估 | SA-MPNN 在相关性指标上优于 ThermoMPNN | 多个 benchmark 数据集，对比 ThermoMPNN | "modest but consistent gains" | 融合序列信息可提升预测精度 | 提升幅度有限，非颠覆性改进 | 摘要 |
| 筛选导向评估 | SA-MPNN 在突变体筛选场景中表现更好 | 筛选导向的评估协议 | "particularly noticeable improvements" | 序列信息对排序/筛选任务尤其有益 | 未提供具体数值 | 摘要 |
| AtBgl1A 湿实验验证 | SA-MPNN 预测的高排名突变体具有实际热稳定性提升 | 选择 top-ranking 突变体，测定 Tm | 最优突变体 GC20：Tm = 76.98°C，较野生型提升 5.97°C | 计算预测可指导实际蛋白质工程 | 仅一个蛋白案例，泛化性有限 | 摘要 |

---

## 11 结论正确解读

- **任务范围**：仅针对单点突变（single-point mutations）的热稳定性预测，不涉及多突变组合、插入/删除、或非热力学性质（如动力学稳定性、溶解度）。
- **oracle/真值输入**：训练和评估使用实验测定的 ΔΔG/Tm 数据（Megascale 及 benchmarks），湿实验验证使用差示扫描量热法（DSC）或类似技术测定的 Tm 值。
- **端到端状态**：模型是端到端训练的，但推理时需要输入蛋白质三维结构（非仅序列），因此依赖结构可用性。
- **算力成本**：摘要强调 "lightweight"，但未给出具体参数量或推理时间对比。
- **历史数据依赖**：训练数据 Megascale 的覆盖范围（序列多样性、突变类型分布）未在摘要中说明，可能影响对低同源性蛋白的泛化。
- **模型依赖**：依赖 ESM2 和 ThermoMPNN 的预训练表示质量；若更换 backbone 模型，性能可能变化。
- **最难情形**：摘要未讨论模型在哪些情况下失败（如对极端突变、非标准残基、或金属蛋白等特殊体系的表现）。
- **群体/领域边界**：湿实验仅验证了一个蛋白（AtBgl1A），且为嗜热菌来源的糖苷酶，不代表所有蛋白类型。
- **不确定性**：未报告预测置信区间或不确定性估计。

**有边界的复述**：SA-MPNN 在单点突变热稳定性预测任务上，通过在 ThermoMPNN 中引入 ESM2 序列表示和自注意力融合，在多个 benchmark 上获得了一致但幅度有限的精度提升，并在一个 β-葡萄糖苷酶的湿实验验证中展示了实际应用潜力。

---

## 12 作者自认局限

在提供的摘要材料中，未发现作者明确承认的局限。摘要未包含 Limitations 或 Future Work 部分。

**作者提及的相关约束**（非正式局限）：
- 摘要中 "modest but consistent gains" 的表述暗示作者承认提升幅度有限。
- 湿实验仅针对单一蛋白（AtBgl1A），作者未声称跨蛋白泛化性。

---

## 13 批判性分析

| [Analysis] 观察 | 潜在问题或替代解释 | 为何重要 | 如何检验 | 依据 |
|----------------|-------------------|---------|---------|------|
| 提升幅度 "modest" 但声称 "consistent" | 可能仅在特定 benchmark 上一致，或提升在统计上显著但实际效果微小 | 决定该方法是否值得在实际工程中替代 ThermoMPNN | 检查各 benchmark 的具体提升幅度及显著性检验 | 摘要原文 "modest but consistent gains" |
| 自注意力融合策略的选择依据不透明 | 作者称评估了多种融合策略，但未说明评估标准（是验证集性能？还是特定指标？） | 融合策略选择可能过拟合特定 benchmark | 要求提供各融合策略的完整对比数据 | 摘要原文 "By evaluating various feature fusion strategies" |
| 湿实验仅验证一个蛋白 | 单一案例无法排除偶然性，且 AtBgl1A 是嗜热酶，其突变效应分布可能不具代表性 | 泛化性是计算预测方法的核心问题 | 在多个不同家族、不同热稳定性范围的蛋白上重复湿实验验证 | 摘要仅提及 AtBgl1A |
| 未报告与物理模拟方法（如 FoldX、MD）的对比 | 仅与 ThermoMPNN 对比，未说明相对传统物理方法的优势 | 用户需要知道新方法相对所有可用工具的定位 | 增加与 FoldX、Rosetta、MD 的 benchmark 对比 | 摘要未提及物理基线 |
| "Lightweight" 声明缺乏量化 | 未给出参数量、推理时间、显存占用等具体数据 | 轻量级是实际部署的重要考量 | 要求提供模型大小和推理效率数据 | 摘要原文 "lightweight" |
| 未讨论失败案例 | 未说明模型在哪些情况下预测失败或偏差大 | 了解失败模式对实际使用至关重要 | 分析预测误差最大的突变类型/蛋白类别 | 摘要未提及 |

---

## 14 学到什么

**Agent 提炼的知识候选**：

1. **模态融合的"动态注意力"范式**：当融合结构信息（来自 IFM/MPNN）和序列进化信息（来自 pLM）时，自注意力机制允许模型按残基位置动态分配权重。这一范式可直接迁移到：
   - 蛋白质-配体结合亲和力预测（融合结构 + 序列进化特征）
   - 蛋白质溶解度/表达量预测
   - 抗体可开发性评估

2. **"轻量级增强"策略**：在已有 SOTA 模型（ThermoMPNN）上做最小侵入式修改（添加融合模块），而非从零设计新架构。这种策略可降低训练成本、保留预训练表示，适用于资源受限的课题组。

3. **IFM 的隐空间偏置**：逆折叠模型的表示偏向"可设计性"而非"稳定性"，这解释了为何需要外部序列模型补充信息。这一洞察提示：在使用 IFM 做性质预测时，应考虑其训练目标带来的表示偏置。

4. **湿实验闭环验证的价值**：计算预测 + 湿实验验证（Tm 测定）的闭环是最有说服力的证据链。对于工程应用导向的方法，即使计算指标提升有限，湿实验的成功案例也能支撑方法的实用价值。

5. **筛选导向评估的重要性**：作者特别强调在 "screening-oriented evaluations" 中的提升，提示在突变效应预测中，排序/筛选能力（而非绝对误差）可能更贴近实际应用需求。这一评估视角可迁移到其他设计任务。

6. **Megascale 数据集的利用**：大规模训练数据（Megascale）与轻量级模型的组合，提示数据规模可能比模型复杂度更重要。

---

## 15 与已有知识连接

1. **ThermoMPNN (2023)**：本文的直接基线和 backbone。ThermoMPNN 是 ProteinMPNN 的微调版本，专门用于热稳定性预测。本文在其基础上添加序列融合模块。

2. **ProteinMPNN (Dauparas et al., 2022)**：逆折叠模型的代表，本文的 IFM 分支继承其 MPNN 架构思想。

3. **ESM2 (Lin et al., 2023)**：本文使用的 pLM。ESM2 是 Meta 发布的蛋白质语言模型，其表示已被广泛用于各种下游任务。

4. **Megascale (Tsuboyama et al., 2023)**：大规模突变稳定性数据集，本文的训练数据来源。该数据集包含约 50 万突变体的稳定性数据。

5. **AlphaFold 系列 (Jumper et al., 2021)**：虽然本文未直接使用 AlphaFold，但其预测的结构可作为 SA-MPNN 的输入，形成"AlphaFold 预测结构 → SA-MPNN 稳定性预测"的管线。

6. **其他 pLM + 结构融合方法**：如 ESM-IF (Hsu et al., 2022)、ProstT5 等，本文与这些方法共享"序列-结构融合"的核心思想，但应用场景不同（稳定性预测 vs 结构预测/设计）。

7. **DDG 预测的传统方法**：FoldX、Rosetta、MD 模拟等物理方法，本文未与之直接对比，但可作为基线参考。

8. **[Analysis] 候选方向**：本文的融合策略可推广到其他 IFM（如 ESM-IF、FrameFlow）与其他 pLM（如 ProtT5、Ankh）的组合，形成"IFM + pLM"通用框架。

---

## 16 研究想法

**Agent 生成的研究候选**：

### 候选 1：多 pLM 集成融合框架
- **名称**：Multi-pLM Ensemble Fusion for Stability Prediction
- **来源局限/观察**：本文仅使用 ESM2 一种 pLM，不同 pLM 编码的进化信息可能互补（如 ESM2 擅长残基级上下文，ProtT5 擅长跨物种比对）
- **核心假设**：集成多种 pLM 表示可进一步提升稳定性预测精度
- **相对本文的增量**：将单 pLM 融合扩展为多 pLM 集成，探索表示多样性带来的增益
- **初步方法**：在 SA-MPNN 框架中并行接入 ESM2、ProtT5、Ankh 三个 pLM，通过注意力机制融合三种序列表示
- **验证方式**：在多个 benchmark 上对比单 pLM vs 多 pLM 的性能
- **可能的失败模式**：多 pLM 表示冗余度高，集成增益有限；计算成本增加
- **创新状态**：unverified

### 候选 2：不确定性感知的稳定性预测
- **名称**：Uncertainty-Aware ThermoMPNN with Evidential Deep Learning
- **来源局限/观察**：本文未报告预测置信度，而工程应用中需要知道哪些预测可靠
- **核心假设**：在 SA-MPNN 中引入 evidential deep learning 或 MC-dropout，可输出预测不确定性，且不确定性估计与真实误差相关
- **相对本文的增量**：增加不确定性输出维度，提升实际工程可用性
- **初步方法**：在 SA-MPNN 的预测头中引入 evidential 回归损失，训练时同时优化预测值和证据参数
- **验证方式**：检查不确定性校准曲线（reliability diagram），并测试在低不确定性子集上的精度提升
- **可能的失败模式**：不确定性估计不校准，或引入额外训练难度
- **创新状态**：unverified

### 候选 3：从单点突变扩展到多突变组合预测
- **名称**：SA-MPNN-Multi: Combinatorial Mutation Effect Prediction
- **来源局限/观察**：本文仅处理单点突变，但实际蛋白质工程常需组合多个突变
- **核心假设**：SA-MPNN 的融合表示可扩展为预测多突变组合效应（含上位效应）
- **相对本文的增量**：将单点预测扩展为组合预测，覆盖更实际的工程需求
- **初步方法**：在 SA-MPNN 中引入突变组合编码器，或在融合表示上做 pairwise interaction 建模
- **验证方式**：使用多突变 benchmark（如 ProTherm 中的组合突变数据）评估
- **可能的失败模式**：组合空间指数增长，数据稀疏导致过拟合
- **创新状态**：unverified

### 候选 4：结构预测误差对稳定性预测的影响分析
- **名称**：Robustness of Stability Prediction to Structural Noise
- **来源局限/观察**：SA-MPNN 需要结构输入，但实际应用中结构可能来自 AlphaFold 预测（含误差）
- **核心假设**：SA-MPNN 对结构噪声的鲁棒性有限，且不同区域（loop vs core）敏感性不同
- **相对本文的增量**：系统分析结构质量对预测的影响，提出鲁棒性改进策略
- **初步方法**：对输入结构添加扰动（如 MD 模拟采样、AlphaFold pLDDT 加权），评估预测稳定性
- **验证方式**：在 benchmark 上对比原始结构 vs 扰动结构的预测一致性
- **可能的失败模式**：扰动模拟不真实，结论不具代表性
- **创新状态**：unverified

### 候选 5：融合表示的可解释性分析
- **名称**：Interpretable Fusion: What Does the Attention Learn?
- **来源局限/观察**：自注意力融合机制的黑箱性质限制了模型的可解释性
- **核心假设**：注意力权重可揭示哪些残基位点更依赖序列 vs 结构信息，且这些模式与蛋白质功能/结构特征相关
- **相对本文的增量**：从"预测精度"扩展到"机制理解"，为蛋白质工程提供位点级指导
- **初步方法**：分析注意力权重与残基保守性、溶剂可及性、二级结构等的相关性
- **验证方式**：统计显著性检验 + 与已知功能位点对照
- **可能的失败模式**：注意力权重与预测贡献无直接对应关系（attention 可解释性争议）
- **创新状态**：unverified

### 候选 6：跨任务迁移：从热稳定性到结合亲和力
- **名称**：Transfer Learning from Stability to Binding Affinity Prediction
- **来源局限/观察**：SA-MPNN 的融合框架可能泛化到其他蛋白质性质预测任务
- **核心假设**：在稳定性数据上预训练的融合表示可迁移到蛋白质-配体/蛋白质-蛋白质结合亲和力预测
- **相对本文的增量**：验证融合框架的通用性，扩展应用范围
- **初步方法**：在 SA-MPNN 框架上更换预测头和训练数据，进行迁移学习
- **验证方式**：在结合亲和力 benchmark（如 SKEMPI、PDBbind）上评估
- **可能的失败模式**：稳定性与结合亲和力的物理基础不同，迁移增益有限
- **创新状态**：unverified